{
  "name": "foodx-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FooDX corporate site (static) with a Vercel serverless contact endpoint (dependency-free, uses global fetch)."
}
